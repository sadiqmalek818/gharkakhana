// ---------------------------------------------------------------------------
// GEMINI (AI CHAT ASSISTANT) CONFIG
// ---------------------------------------------------------------------------
// 1. Get a free API key: https://aistudio.google.com/app/apikey
// 2. Paste it below in place of "YOUR_GEMINI_API_KEY_HERE".
// 3. IMPORTANT — restrict the key so no one else can use it from your bill:
//      Google Cloud Console → APIs & Services → Credentials → click your key
//      → "Application restrictions" → "Websites" → add:
//        sadiqmalek818.github.io/*
//      This makes the key only work when called from your own site, even
//      though it's visible in the page's source code (normal for client-side
//      AI widgets — the referrer restriction is what actually protects it).
// ---------------------------------------------------------------------------

window.GEMINI_API_KEY = "AQ.Ab8RN6IITtQjbzez-w861-MUacAax6VDjyaayMOxwPVCfWMGAg";
window.GEMINI_CONFIGURED = Boolean(window.GEMINI_API_KEY && window.GEMINI_API_KEY !== "");
